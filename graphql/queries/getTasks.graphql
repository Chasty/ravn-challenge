query GetTasks($input: FilterTaskInput!) {
  tasks(input: $input) {
    assignee {
      id
      fullName
    }
    creator {
      fullName
    }
    dueDate
    id
    name
    pointEstimate
    status
    tags
    createdAt
    position
  }
}
