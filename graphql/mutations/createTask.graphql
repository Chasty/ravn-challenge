mutation CreateTask($input: CreateTaskInput!) {
  createTask(input: $input) {
    assignee {
      fullName
    }
    creator {
      fullName
    }
    dueDate
    id
    name
    pointEstimate
    status
    tags
    createdAt
    position
  }
}
